# Your OpenAI API key (required for AI features; mock fallback used if unset)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI model to use for itinerary generation and chatbot
OPENAI_MODEL=gpt-4o-mini

# Application environment (development | production)
APP_ENV=development
